 /*import { View, Button, StyleSheet } from 'react-native';
import { playRelax, stopRelax } from '../../utils/audio';

export default function RelaxScreen() {
  return (
    <View style={styles.container}>
      <Button
        title="Purring"
        onPress={() =>
          playRelax(require('../../assets/sounds/purr.mp3'))
        }
      />
      <Button
        title="Rain"
        onPress={() =>
          playRelax(require('../../assets/sounds/rain.mp3'))
        }
      />
      <Button
        title="Birds"
        onPress={() =>
          playRelax(require('../../assets/sounds/birds.mp3'))
        }
      />
      <Button title="Stop" color="red" onPress={stopRelax} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    gap: 12,
  },
}); */
